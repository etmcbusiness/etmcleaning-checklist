/**
 * Dashboard link to your other app (separate repo / GitHub Pages site).
 *
 * 1. Set `url` to the full live address, e.g. https://your-username.github.io/your-other-repo/
 *    Or, if you copy that app into a subfolder of this project: ./your-folder/index.html
 * 2. Set `label` to the button text shown on the home screen.
 * 3. Use `openInNewTab: true` when the URL is another website (default). Use false for a path on this same site.
 *
 * Leave `url` empty to hide the “More tools” block until you’re ready.
 */
window.ETM_DASHBOARD_LINK = {
  url: '',
  label: 'Open my other tool',
  openInNewTab: true
};
